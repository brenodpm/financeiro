mod ofx_repy;
mod file_repy;
mod categorizador_repy;
mod lancamento_repy;
mod regra_repy;
mod categoria_repy;

pub use file_repy::arq_ler_windows_1252;
/*
trait SubVec<T> {
    fn sub_vec(self) -> Self;
}

impl<T>  SubVec<T> for Vec<T> {
    fn sub_vec(mut self) -> Self{
        self.remove(0);
        self
    }
} 
*/